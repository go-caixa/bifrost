# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Development Migration Up

on:
  workflow_dispatch:
    inputs:
      migrate:
        type: choice
        description: What kind of migration want to run
        options:
        - up
        - down

env:
  DB_HOST: ${{ secrets.DB_HOST_DEV }}
  DB_PASSWORD: ${{ secrets.DB_PASSWORD_DEV }}
  DB_NAME: caixa
  DB_USER: caixa
  DB_SSLMODE: disable

jobs:
  migrations:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: 1.18

    - name: Install Go sql-migrate
      run: go install github.com/rubenv/sql-migrate/...@latest
      
    - name: Run Migrations
      run: sql-migrate ${{ github.event.inputs.migrate }} -config=internal/config/dbconfig.yml -env=remote
